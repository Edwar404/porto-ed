let text = document.querySelector(".text");

let typed = new Typed(text, {
    strings: [
        "Full Stack Developer",
        "Front-End Developer",
        "Back-End Developer",
    ],
    speed: 50,
    backSpeed: 50,
    backDelay: 1000,
    loop: true,
});
